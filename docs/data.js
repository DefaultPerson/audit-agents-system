// Autonomous Audit Agents — pipeline concept data
// Real-world-shaped loop: EVM snapshot -> testnet-proven exploit.

const STAGES = [
  {
    id: "snapshot",
    num: "01",
    name: "Snapshot & Filter",
    role: "Target Sourcing",
    tagline: "Sync a full EVM snapshot, then mine it for the most promising prey: closed-source contracts, old and untouched, that still hold real funds.",
    accent: "green",
    icon: `<path d="M3 7v10a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V7"/><path d="M3 7l9 5 9-5"/><path d="M12 2v10"/>`,
    mechanics: [
      ["full-node snapshot", "the whole chain, local & queryable offline — every deployed contract"],
      ["criteria filter", "rank by closed-source + age + idle TVL — old & unaudited scores highest"],
      ["candidate queue", "the narrowed set is streamed downstream"],
    ],
    metric: "candidates",
    code: [
      "snap    = evm.load_snapshot('mainnet')",
      "targets = (snap.contracts",
      "    .where(verified=False)        # closed-source",
      "    .where(age_days__gt=3*365)    # old",
      "    .where(balance_usd__gt=50_000)",
      "    .order_by('-balance_usd'))",
    ],
    log: [
      "snapshot synced  block 21_482_119",
      "contracts indexed: 18,402,114",
      "filter closed-source + age>3y -> 214,880",
      "+ idle TVL > $50k -> 1,932 candidates  ★",
    ],
  },
  {
    id: "decompile",
    num: "02",
    name: "Decompile",
    role: "Dedaub Recovery",
    tagline: "Push each candidate through Dedaub — the strongest EVM decompiler — and recover code that often reads almost like the original source.",
    accent: "green",
    icon: `<path d="M9 6 4 12l5 6"/><path d="m15 6 5 6-5 6"/>`,
    mechanics: [
      ["Dedaub engine", "lifts raw bytecode into high-level, typed pseudo-Solidity"],
      ["selectors + storage", "function signatures and storage layout reconstructed"],
      ["clean enough to read", "good enough for an agent to reason over directly"],
    ],
    metric: "decompiled",
    code: [
      "src   = dedaub.decompile(target.bytecode)",
      "# typed, named, control-flow recovered",
      "funcs = src.external_functions()  # entrypoints",
      "state = src.storage_layout()",
    ],
    log: [
      "POST dedaub.decompile  4128 bytes",
      "recovered 11 functions / 14 slots",
      "names resolved: mint() refreshAccount() ..",
      "readability: high  ✓",
    ],
  },
  {
    id: "swarm",
    num: "03",
    name: "Agent Swarm",
    role: "Parallel Auto-Research",
    tagline: "Spawn a swarm of long-running agents across Opus, GLM and Codex. They hammer every function in parallel — and cross-check each other, so only findings more than one model reaches survive.",
    accent: "cyan",
    icon: `<rect x="3" y="3" width="6" height="6" rx="1.4"/><rect x="15" y="3" width="6" height="6" rx="1.4"/><rect x="9" y="15" width="6" height="6" rx="1.4"/><path d="M6 9v3a2 2 0 0 0 2 2h2m8-5v3a2 2 0 0 1-2 2h-2"/>`,
    mechanics: [
      ["fan-out per function", "one or more agents assigned to every entrypoint"],
      ["multi-model", "the same target probed by different models at once"],
      ["long-horizon loop", "agents keep forming, testing and discarding hypotheses"],
    ],
    metric: "hypotheses",
    code: [
      "swarm = AgentSwarm(['Opus','GLM','Codex'])",
      "for fn in funcs:",
      "    swarm.spawn(task=f'break {fn.name}',",
      "        budget='long', parallel=True)",
      "hyps = swarm.collect()      # ranked findings",
      "hyps = swarm.cross_check(hyps)  # peer-verify",
    ],
    log: [
      "spawning 11 fns x 3 models = 33 agents",
      "Opus: price() overflow -> 0  ★★",
      "GLM independently re-derives H2  match ✓",
      "Codex writes PoC & attacks it  holds ✓",
    ],
  },
  {
    id: "validate",
    num: "04",
    name: "Validate",
    role: "Creative Triage",
    tagline: "The creative step. Promising hypotheses get pressure-tested every which way — re-reasoned, cross-checked, probed — and the noise is thrown out.",
    accent: "amber",
    icon: `<path d="M12 3 4 6v5c0 5 3.5 8 8 10 4.5-2 8-5 8-10V6l-8-3Z"/><path d="m9 12 2 2 4-4"/>`,
    mechanics: [
      ["multiple angles", "static recheck, fresh reasoning, quick sim probes"],
      ["cross-model vote", "findings both models reach independently rank highest"],
      ["keep / discard", "only the survivors move on — most don't"],
    ],
    metric: "survivors",
    code: [
      "for h in hyps:",
      "    h.score = (recheck(h)",
      "             + cross_model_agree(h)",
      "             + quick_sim_probe(h))",
      "keep = [h for h in hyps if h.score >= BAR]",
    ],
    log: [
      "validating 41 candidate hypotheses",
      "cross-model agreement on H2 (overflow)",
      "discarded 38  (noise / unreachable)",
      "3 survive -> exploit queue  ✓",
    ],
  },
  {
    id: "exploit",
    num: "05",
    name: "Exploit & Testnet",
    role: "PoC on Testnet",
    tagline: "For the best survivors, write the actual exploit, deploy it, and fire it on a testnet fork to prove the bug is real — safely, before anything touches mainnet.",
    accent: "red",
    icon: `<path d="M13 2 4 14h7l-1 8 9-12h-7l1-8Z"/>`,
    mechanics: [
      ["write the exploit", "agent drafts a runnable PoC for each kept finding"],
      ["deploy to testnet", "a fork reproduces real state — no mainnet risk"],
      ["prove impact", "run it, measure the balance delta, confirm it drains"],
    ],
    metric: "confirmed",
    code: [
      "poc    = agent.write_exploit(keep[0])",
      "fork   = testnet.fork(at_block=h.block)",
      "before = fork.balance(attacker)",
      "fork.run(poc)",
      "assert fork.balance(attacker) > before  # drained",
    ],
    log: [
      "writing exploit for H2 ..",
      "deploy -> testnet fork @ 21_482_004",
      "running .. balance Δ = +1,204.6 ETH",
      "confirmed  ✓  (testnet only)",
    ],
  },
];

window.STAGES = STAGES;
